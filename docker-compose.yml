services:
  docs:
    image: nginx:alpine
    container_name: ils-docs
    restart: always
    volumes:
      - .:/usr/share/nginx/html:ro
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
